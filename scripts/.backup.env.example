# Copy this file to `.backup.env` (gitignored) and fill in real values.
# Never commit `.backup.env` — it contains credentials.

PGHOST=aws-1-ap-northeast-2.pooler.supabase.com
PGPORT=5432
PGUSER=postgres.<project-ref>
PGPASSWORD=
RETENTION=12

# Optional: mirror each dump into the private db-backups bucket.
# UPLOAD_TO_BUCKET=1
# SERVICE_ROLE_KEY=