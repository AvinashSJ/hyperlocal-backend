"use client";

// Bootstrap JS intentionally not loaded — all interactive elements
// use React state (no data-bs-* attributes). The CSS import in
// globals.css still provides all Bootstrap styling.

export default function BootstrapClient() {
  return null;
}
